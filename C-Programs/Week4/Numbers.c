#include<stdio.h>

// Counter Controlled Repetition

int main()
{
  // Intialization
  int counter = 1;
  // Repetition condition
  while (counter <= 10)
  {
    printf("%d\n", counter);
    // Iteration (Increment)
    counter++;
  }
} 
