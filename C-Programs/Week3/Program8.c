#include<stdio.h>
 
   /* TYPE CASTING FOR FITTING FLOAT VALUE IN INT */
	
int main()
{
	int x;
	float r = 3.0;

	x = (int)(2*r);  /* use nahi pata int ka ? */
	printf("%d is the value of X", x);
	return 0;
}
